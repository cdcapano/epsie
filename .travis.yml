language: python
python:
  - "2.7"
  - "3.6"
install:
  - pip install -r requirements.txt
  - pip install .
  - pip install pytest pytest-cov h5py
  - pip install coveralls
script:
  - pytest --cov=epsie test
after_success:
  - coveralls
